import { AuthCard } from "@/components/forms/auth-card";

export default function ForgotPasswordPage() {
  return <AuthCard mode="forgot" title="Reset password" subtitle="We'll send reset instructions to your email." />;
}
